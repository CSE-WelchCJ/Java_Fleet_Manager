The task is this homework was to demonstrate the use of classes to define objects.  The objects were defined in such a way that two classes would
inhereit from a super-class.  The code then demonstrates method overriding, constructors, accessors, and mutators among other things.

At the time we had not covered using arrays to save objects when created so the Fleet Manager will not remember boats after they are made.  This
was an interesting exercise in using methods across classes.
